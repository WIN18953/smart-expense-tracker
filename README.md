โปรเจ็คนี้จัดทำโดยนักศึกษามหาวิทยาลัยสงขลานครินทร์
1. นางสาวณัฐรินีย์ แพประสิทธิ์ 6810110102
2. นายธนะรัชต์ เทพธัญญ์ 6810110141
3. นายวิญญู สิงห์สาธร 6810110324

   
การทำงานของระบบ Smart Expense Tracker
ระบบ Smart Expense Tracker เป็นแอปพลิเคชันสำหรับบันทึกและจัดการรายรับ–รายจ่าย พัฒนาด้วยภาษา Python โดยใช้เฟรมเวิร์ก Kivy สำหรับสร้างส่วนติดต่อผู้ใช้ (GUI) รองรับการใช้งานหลายภาษา (ไทย–อังกฤษ) และสามารถเปลี่ยนธีมสีระหว่างโหมดมืดและโหมดสว่างได้
ครงสร้างหน้าจอของระบบ (Screen Management)

ระบบใช้ ScreenManager ในการควบคุมการแสดงผลของหน้าจอทั้งหมด 4 หน้าหลัก ได้แก่
HomeScreen – หน้าแสดงข้อมูลสรุปรายรับ รายจ่าย และรายการธุรกรรม
AddScreen – หน้าสำหรับเพิ่มรายการรายรับหรือรายจ่าย
ReportScreen – หน้าแสดงรายงานสรุปรายเดือนพร้อมกราฟ
SettingScreen – หน้าการตั้งค่าภาษา ธีม และการจัดการข้อมูล
การเปลี่ยนหน้าจอทำโดยกำหนดค่า root.manager.current ตามชื่อหน้าจอที่ต้องการ


การจัดเก็บและจัดการข้อมูล (Data Management)
ระบบจัดเก็บข้อมูลรายรับ–รายจ่ายในไฟล์ data.json โดยมีโครงสร้างข้อมูลเป็นรูปแบบ JSON ซึ่งประกอบด้วย
จำนวนเงิน (amount)
ประเภทรายการ (income / expense)
หมวดหมู่ (category)
วันที่ (date)
หมายเหตุ (note)

ฟังก์ชันหลักที่ใช้จัดการข้อมูล ได้แก่
load_data() → โหลดข้อมูลจากไฟล์ data.json
save_data(data) → บันทึกข้อมูลกลับลงไฟล์ data.json
clear_data() → ล้างข้อมูลทั้งหมด
export_data() → ส่งออกข้อมูลเป็นไฟล์ CSV

3️หน้า HomeScreen (หน้าหลักของระบบ)
หน้า HomeScreen ทำหน้าที่แสดงภาพรวมของข้อมูลทั้งหมด โดยประกอบด้วย
🔹 สรุปรายรับ รายจ่าย และยอดคงเหลือ
คำนวณจากข้อมูลใน data.json
แสดงผลด้วย Label 3 ช่อง ได้แก่ Income, Expense และ Balance
รองรับการเปลี่ยนสกุลเงินตามการตั้งค่า
🔹 ตัวกรองตามเดือน
ใช้ Spinner เพื่อเลือกดูข้อมูลรายเดือนหรือทั้งหมด
ระบบจะกรองข้อมูลจากวันที่ (เดือน/ปี) ของรายการ
🔹 แสดงรายการธุรกรรม
ใช้ TransactionItem ซึ่งเป็น Custom Widget
แสดงข้อมูลวันที่ หมวดหมู่ จำนวนเงิน และหมายเหตุ
แยกสีระหว่างรายรับ (สีเขียว) และรายจ่าย (สีแดง)
มีปุ่มลบ (Delete) สำหรับลบรายการนั้น ๆ

หน้า AddScreen (เพิ่มรายการ)
หน้า AddScreen ใช้สำหรับเพิ่มข้อมูลรายรับหรือรายจ่ายใหม่ โดยผู้ใช้สามารถกรอกข้อมูลดังนี้
จำนวนเงิน
หมายเหตุ
ประเภท (รายรับ / รายจ่าย)
หมวดหมู่
วันที่ (หากไม่กรอก ระบบจะใช้วันที่ปัจจุบัน)
เมื่อกดปุ่ม Save
ระบบจะตรวจสอบความถูกต้องของข้อมูล
บันทึกข้อมูลลง data.json
กลับไปยังหน้า HomeScreen และรีเฟรชข้อมูลทันที

หน้า ReportScreen (รายงานสรุป)
หน้า ReportScreen แสดงรายงานรายรับ–รายจ่ายตามเดือนที่เลือก โดยมีการทำงานดังนี้
🔹 สรุปตัวเลขรายเดือน
คำนวณรายรับ รายจ่าย และยอดคงเหลือของเดือนที่เลือก
แสดงผลด้วย Label
🔹 แสดงกราฟ
ใช้ไลบรารี matplotlib
แสดงกราฟแท่งเปรียบเทียบรายรับและรายจ่าย
ปรับสีกราฟและพื้นหลังตามธีม (Dark / Light)
แปลงกราฟเป็นรูปภาพและแสดงผลในแอปด้วย CoreImage

หน้า SettingScreen (การตั้งค่า)
หน้า SettingScreen ใช้สำหรับตั้งค่าการใช้งานระบบ ได้แก่
🔹 การตั้งค่ารูปลักษณ์
เปลี่ยนภาษา (ไทย / อังกฤษ)
เปลี่ยนธีม (โหมดมืด / โหมดสว่าง)
🔹การตั้งค่าทั่วไป
เปลี่ยนสกุลเงิน (THB, USD, EUR, JPY)
🔹 การจัดการข้อมูล
ส่งออกข้อมูลเป็นไฟล์ CSV
ล้างข้อมูลทั้งหมดในระบบ

การจัดการภาษา (Multi-language Support)
ระบบรองรับ 2 ภาษา คือ ภาษาไทย และภาษาอังกฤษ โดยใช้พจนานุกรม TRANSLATIONS
เมื่อผู้ใช้เปลี่ยนภาษา ระบบจะ
อัปเดตข้อความทั้งหมดใน UI
เปลี่ยนฟอนต์ให้เหมาะสมกับภาษา (Tahoma สำหรับภาษาไทย)

การจัดการธีมสี (Theme Management)
ระบบรองรับ Dark Mode และ Light Mode โดยกำหนดสีผ่าน ColorProperty เช่น
สีพื้นหลัง
สีตัวอักษร
สีการ์ด
สีช่องกรอกข้อมูล
เมื่อผู้ใช้เปลี่ยนธีม ระบบจะเรียกฟังก์ชัน update_theme_colors() เพื่ออัปเดตสีทั้งหมดแบบเรียลไทม์

สรุปภาพรวมระบบ
Smart Expense Tracker เป็นแอปพลิเคชันที่ช่วยให้ผู้ใช้สามารถ
บันทึกรายรับ–รายจ่าย
ดูสรุปและรายงานข้อมูลทางการเงิน
ปรับแต่งภาษา ธีม และสกุลเงิน
จัดการข้อมูลได้อย่างเป็นระบบ
โดยระบบถูกออกแบบให้ใช้งานง่าย มีโครงสร้างชัดเจน และสามารถขยายฟังก์ชันเพิ่มเติมได้ในอนาคต
